apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  name: claude-auth
  namespace: claude-proxy
spec:
  encryptedData:
    credentials.json: AgReplaceMeWithKubesealOutput
    settings.json: AgReplaceMeWithKubesealOutput
  template:
    metadata:
      name: claude-auth
      namespace: claude-proxy
    type: Opaque
